varying float ptSize;

void main() {
  vec2 coord = gl_PointCoord * 2.0 - 1.0;
  vec4 color;
  if((coord.x * coord.x + coord.y * coord.y) < 1.0) {
    color = vec4(vec3(0.5), 0.7);
  } else {
    discard;
  }
  gl_FragColor = color;
}